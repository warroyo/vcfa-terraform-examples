# Supervisor Namespace Registration for ArgoCD

This module handles registering a supervisor namespace with an argocd instance. This can work across namespaces as well. for example you can have argocd in one NS and register another to it.

